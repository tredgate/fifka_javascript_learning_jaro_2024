// * Definice konstantní funkce, která zapisuje text do konzole na základě parametru
const logText = (text) => {
  console.log("Předaný argument: " + text);
};

// * Volání funkce logText s argumentem
logText("Programuji v Javascriptu!");
